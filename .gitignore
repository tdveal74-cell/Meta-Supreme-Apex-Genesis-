# Dependencies
node_modules/
.pnpm-store/

# Builds
.next/
out/
dist/
*.tsbuildinfo

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Environment — never commit real secrets
.env
.env.local

# OS / editor
.DS_Store
*.log

.claude/settings.local.json

# Workflow and agent worktrees are never repository content
.claude/worktrees/

# scroll-craft build workspace. The skill resolves it to <project root>/scrollcraft
# unless SCROLLCRAFT_HOME or a .scrollcraft.json says otherwise. A single build
# with video legs is 300MB+, and committing one puts it in history permanently.
scrollcraft/
